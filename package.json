{
  "name": "timeline",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm i && node server.js",
    "heroku-postbuild": "cd client && npm i && npm run-script build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "express-validator": "^5.3.1"
  },
  "devDependencies": {
    "eslint": "5.12.0"
  },
  "engines": {
    "node": "10.x"
  }
}
